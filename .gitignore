node_modules
dist
artifacts
.DS_Store
*.log
test-results
playwright-report
.vite
release

# large raw screenshot captures (curated gallery is committed)
submission/raw-*/

# raw video frames (the encoded film is committed, the frames are not)
submission/video/work/
