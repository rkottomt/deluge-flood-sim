Baked preset data (pittsburgh, johnstown, ellicott) — sources and terms
=======================================================================

Each preset directory is generated by scripts/bake-presets.ts from these public-domain U.S. government sources:

  elevation.f32  USGS 3D Elevation Program (3DEP), via the National Map 3DEPElevation ImageServer.
                 Public domain. Hydro-conditioned (river channels burned) by the bake script.
  imagery.jpg    USDA National Agriculture Imagery Program (NAIP), via the USGS National Map
                 USGSNAIPPlus ImageServer. Public domain. 4096 x 4096, stitched from 2048 x 2048 exports.
  roads.json     U.S. Census Bureau TIGER/Line roads, via TIGERweb. Public domain.
  buildings.json Building footprints and heights from OpenStreetMap, fetched once by scripts/bake-buildings.ts from
                 the OSM API 0.6 /map endpoint. (c) OpenStreetMap contributors, Open Database License (ODbL) 1.0 —
                 https://www.openstreetmap.org/copyright. ODbL permits use, redistribution and derived works in a
                 public repository, provided the source is attributed and any DERIVED DATABASE is offered under the
                 same licence: buildings.json is such a derived database and is offered under ODbL 1.0. The other
                 baked files are public-domain U.S. government data and are unaffected by this.
                 Heights are recorded with their provenance per building: measured from lidar (see LIDAR below), an
                 OSM `height` tag (surveyed), a `building:levels` floor count converted at the metres-per-storey
                 fitted to the buildings that carry both tags (about 3.2 m low-rise, 3.95 m high-rise), or an
                 estimate from the building's class and footprint area. Estimates are flagged 'estimated' in the data
                 and must be described as estimates. For PITTSBURGH the great majority are now measured, not
                 estimated: of 45 084 footprints, 41 527 (92.1 %) carry a roof height measured from lidar, 65 an OSM
                 height tag, 64 a floor count, and 3 428 (7.6 %) remain estimates. Johnstown and Ellicott have not
                 been re-baked against lidar and are still dominated by estimates.
                 Where OpenStreetMap has mapped very few buildings (Johnstown: 16 per km2, versus 700 in Pittsburgh),
                 the gaps are filled from Microsoft's Global ML Building Footprints — machine-extracted from
                 satellite imagery, with a per-building height measured from stereo imagery. Also ODbL 1.0, so the
                 licence above is unchanged: https://github.com/microsoft/GlobalMLBuildingFootprints. A footprint is
                 added only where OpenStreetMap has none, so hand-mapped buildings are never replaced, and these
                 heights are flagged 'remote' in the data (measured remotely, not surveyed).

  LIDAR ROOF HEIGHTS (Pittsburgh)
                 USGS 3D Elevation Program (3DEP) airborne lidar, fetched from the AWS Open Data mirror
                 s3://usgs-lidar-public as Entwine Point Tile (EPT) collections. PUBLIC DOMAIN (USGS 3DEP); no
                 attribution is required beyond the credit line above, and this does not affect the ODbL terms on the
                 OpenStreetMap footprints — only the heights attached to them come from lidar.
                 Projects: PA_WesternPA_2_2019 covers the domain west of mercator x -8899335 and PA_WesternPA_1_2019
                 the remaining eastern strip; both are needed, as neither covers the whole 8 km square. Acquired
                 2019-11-26 to 2020-02-23 (from the point GpsTime range) — a leaf-off winter flight, which is the
                 favourable season for seeing roofs through trees. Density over the domain is about 11.8 returns per
                 square metre.
                 Method (scripts/bake-buildings.ts --lidar, tooling and full logs in artifacts/gfx-lidar): a digital
                 surface model is rasterised from the highest non-noise return per cell on a 0.977 m grid that is
                 exactly 8x the preset grid and shares its origin. Both the EPT builds and the preset grid are
                 EPSG:3857, so no horizontal reprojection is involved. Each footprint is eroded by one DSM cell and
                 its roof elevation taken as the HIGHEST ROOF PLANE — the median of the DSM cells within 5 m of the
                 footprint's 99th percentile, where that plane covers at least 10 cells, else the 85th percentile of
                 the whole footprint. A footprint-wide percentile alone is not enough: a downtown tower is mapped on
                 the whole city block it stands in, so a plain median returns the podium (it reads the Cathedral of
                 Learning at 21 m). The stored height is that roof elevation minus the footprint's `base`, so the
                 roof lands at the elevation the lidar saw it at.
                 Datum: lidar Z is NAVD88 metres, and so is elevation.f32. This was measured, not assumed — the
                 lidar ground surface minus elevation.f32 has a median of -0.03 m and a median absolute deviation of
                 0.32 m over the domain, with 94 % of cells inside 2 m, which also confirms the horizontal
                 registration.
                 Honest limits. These projects are classified only to ground, water, bridge deck and noise — there is
                 NO vegetation or building class — so a tree crown over a small building can be measured as its roof.
                 Such footprints are detected geometrically (a roof returns all its energy from one surface, a crown
                 from many) and keep their previous value instead: 2 772 footprints were rejected this way, along with
                 737 whose measured height came out below 2 m (footprints over water, and open-air stadium fields
                 where the lidar correctly sees grass), 41 too small to sample, and 6 setback towers where a surveyed
                 OSM height is the better number for a single-extrusion renderer. Measured heights run about 1.4 m
                 above OSM's surveyed tags (median; MAD 1.2 m over 1 311 buildings), which is what a parapet and roof
                 plant look like from above. Where a building's physical top is not its roof the lidar reports the
                 top: One PPG Place measures 193.5 m to its spires against a 166 m roof, and the Grant Building
                 137 m to its roof against a 149 m architectural height including the beacon mast.
                 NOTE ON THE FLAG: lidar roofs are flagged 'remote' (measured remotely, not surveyed) because that is
                 the only value the 2-bit provenance field carries that means this. It is therefore shared with the
                 Microsoft stereo-imagery heights described above. Pittsburgh contains no Microsoft footprints, so
                 there the flag is unambiguous; separating the two needs a wider field.
  meta.json      Scenario text and parameters. Historic stages and flows come from NOAA NWS (gauge PTTP1) and
                 USGS peak-flow records (Stonycreek River at Ferndale, 03040000; Patapsco River at Hollofield,
                 01589000). Johnstown's 1889 flood: Lake Conemaugh volume and drain time from Coleman, Kaktins &
                 Wojno (2016), "Dam-Breach hydrology of the Johnstown flood of 1889", Heliyon 2(6) e00120; the 1977
                 flood from USGS Open-File Report 78-963. Ellicott City's storms, watershed and branch peak flows:
                 USGS Fact Sheet 2021-3025.

Please credit the agencies when reusing the data: "Elevation: USGS 3DEP · Imagery: USDA NAIP via USGS The National
Map · Roads: U.S. Census Bureau TIGER/Line · Buildings © OpenStreetMap contributors (ODbL)" (also stored as
`attribution` in each meta.json).

Live areas loaded in the app are fetched at runtime and never stored in the repository. They use Esri World Imagery
(© Esri and its imagery providers, under Esri's terms) and, as fallbacks, Mapzen Terrarium elevation tiles (AWS Open
Data) and OpenStreetMap roads (© OpenStreetMap contributors, ODbL).
